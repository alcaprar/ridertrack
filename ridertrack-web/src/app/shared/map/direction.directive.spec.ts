import { DirectionDirective } from './direction.directive';

describe('DirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectionDirective();
    expect(directive).toBeTruthy();
  });
});
